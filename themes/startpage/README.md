This directory previously pointed to a submodule (themes/startpage).
The submodule reference was removed and this folder is now tracked as a normal directory.
Restore the actual theme files here when available.
